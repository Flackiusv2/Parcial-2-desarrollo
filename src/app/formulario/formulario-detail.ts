import { Formulario } from './formulario';

export class FormularioDetail extends Formulario{

    constructor(
        prompt:string,
        format:string
    ){
        super(prompt, format);
    }

}